
// this page inherits from the dashboard specific layout
// the layout itself is pulling in @evenst and @rsvps as
// parrell routes. 
const Home = async () => {
  return <div>home</div>
}

export default Home
